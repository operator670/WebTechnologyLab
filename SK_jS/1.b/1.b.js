let a = Number(prompt("Enter the first number"));
let b = Number(prompt("Enter the second number"));

let sum = a+b;
if(sum % 2===0)
    document.write("The sum of the numbers is even ");
else
    document.write("The sum of the numbers is odd");
